---
- name: "Stop mysql instance"
  command: "docker stop {{ mysql_name }}"
  register: stop_mysql
  become: yes

- debug: var=stop_mysql

- name: "Remove mysql container"
  command: "docker rm {{ mysql_name }}"
  register: remove_mysql
  become: yes

- debug: var=remove_mysql
